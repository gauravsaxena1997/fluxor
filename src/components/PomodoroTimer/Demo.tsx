import React from 'react';
import { PomodoroTimer } from './PomodoroTimer';

const Demo: React.FC = () => {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <PomodoroTimer />
    </div>
  );
};

export default Demo;
